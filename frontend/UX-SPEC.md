# UX-SPEC.md — DartEvent Manager

> Generated by The Wolf (UX/UI Agent) | 2026-03-16
> P Entertainment Corporate Design | Mobile-first | Dark Mode only

---

## 1. Screen Specifications

### 1.1 HomePage (`/`)
**Route:** Shell route (TopBar + BottomNav/TopNav)
**Purpose:** Public tournament overview — players, boards, groups, bracket

**Required Elements:**
- Tournament card (name, date, format, status badge, player count)
- Tab navigation (Spieler / Scheiben / Gruppen / Bracket)
- Player search input
- Player grid with stats (W/L, Average, Highest Out)
- Board list with current/next game
- Group standings table
- Bracket view (KO phase)
- "Beitreten" button (when tournament open)
- "Weitere Turniere" section

**Touch Targets (min 64px):**
- [x] "Beitreten" button — `minHeight: 64` OK
- [ ] Tab buttons — `minHeight: 48` VIOLATION (line 511)
- [x] Player search input — `minHeight: '64px'` OK
- Player cards — not interactive buttons (pe-card-interactive hover only)
- Board cards — Link elements, padding-based height

**States:**
| Element | Normal | Loading | Error | Disabled |
|---------|--------|---------|-------|----------|
| Page | Content | "Lade Turniere..." | - | - |
| Tab buttons | Outlined | - | - | - |
| Join button | Gradient bg | - | - | - |
| Search input | Border | - | - | - |

**Tap Depth:** 0 taps (root page)

---

### 1.2 TournamentPage (`/tournament/:id`)
**Route:** Shell route (sub-page with back button in TopBar)
**Purpose:** Tournament detail — players + bracket

**Required Elements:**
- Tournament name (gradient text), date, format, checkout
- "Jetzt anmelden" link (when open)
- Player list grid
- Bracket component

**Touch Targets:**
- [ ] "Jetzt anmelden" link — uses `py-3` class (~48px), no explicit minHeight VIOLATION (line 49)
- Player cards — display only, not interactive

**States:**
| Element | Normal | Loading | Error |
|---------|--------|---------|-------|
| Page | Content | "Lade..." text | "Turnier nicht gefunden." |
| Anmelden link | Gradient bg | - | - |

**Missing:**
- [ ] No BackButton component used — relies on TopBar back arrow only (acceptable via AppShell)

**Tap Depth:** 1 tap from Home

---

### 1.3 PlayerRegistrationPage (`/tournament/:id/register`)
**Route:** Shell route (sub-page)
**Purpose:** Player registration form

**Required Elements:**
- Tournament info card (name, date, format)
- Form: Vorname, Nickname, Nachname (all required)
- Live name preview
- Submit button with disabled/loading states
- Success view with cancel link
- Info text about cancel link

**Touch Targets:**
- [x] Input fields — `minHeight: '64px'` OK (via `inp` style)
- [x] Submit button — `minHeight: '64px'` OK
- [ ] Success "Zur Turnier-Ubersicht" link — `minHeight: '56px'` VIOLATION (line 120)
- [ ] Success "Link offnen / testen" link — no minHeight VIOLATION (line 112)

**States:**
| Element | Normal | Loading | Error | Disabled | Success |
|---------|--------|---------|-------|----------|---------|
| Submit btn | Gradient | "Wird angemeldet..." + opacity 0.6 | - | Grey bg + not-allowed | - |
| Form | Editable | - | Red error card | - | Success card |
| Page | Form | - | - | - | Success view |

**Tap Depth:** 2 taps (Home > Tournament/Join > Register)

---

### 1.4 CancelRegistrationPage (`/cancel/:token`)
**Route:** Standalone (no shell)
**Purpose:** Cancel tournament registration via personal link

**Required Elements:**
- BackButton + Logo header
- Registration info card (player name, tournament, format)
- Warning text
- Cancel confirmation button
- Success/error/expired states

**Touch Targets:**
- [x] BackButton — `minHeight: '64px'` OK
- [x] Cancel button — `minHeight: '64px'` OK
- [ ] "Zur Startseite" links (error + expired states) — `padding: '12px 24px'`, no minHeight VIOLATION (lines 65, 136, 155)

**States:**
| Element | Normal | Loading | Error | Cancelling | Cancelled |
|---------|--------|---------|-------|------------|-----------|
| Page | Confirm view | "Lade..." | Invalid link card | - | Success card |
| Cancel btn | Red bg | - | - | "Wird abgemeldet..." + opacity 0.7 | - |

**Tap Depth:** 1 tap (direct link)

---

### 1.5 AdminPage (`/admin`)
**Route:** Shell route
**Purpose:** Admin dashboard with tabs (overview, tournaments, players, boards, settings, etc.)

**Note:** File too large to read fully (55k tokens). Analysis based on partial read + known components.

**Required Elements:**
- Login gate (AdminLogin component if not authenticated)
- Tab-based navigation via BottomNav/TopNav
- Tournament management (TournamentManager)
- Player management
- Board management
- Order summary
- Settings

**Known Touch Target Issues (from sub-components):**
- [ ] TournamentManager "+ Neu" button — `px-4 py-2`, no minHeight VIOLATION (TournamentManager.jsx:68)
- [ ] TournamentManager inputs — `minHeight: '48px'` VIOLATION (TournamentManager.jsx:44)
- [ ] TournamentManager "Turnier erstellen" button — `minHeight: '48px'` VIOLATION (TournamentManager.jsx:116)
- [ ] TournamentManager "Turnier starten" button — `minHeight: '48px'` VIOLATION (TournamentManager.jsx:143)

**Tap Depth:** 1 tap (Home > Login/Admin)

---

### 1.6 CurrentGameView (`/board/:boardId`)
**Route:** Standalone (no shell)
**Purpose:** TV/Beamer board view — live game score display

**Required Elements:**
- Header (logo, board number, round label)
- Player panels (name, remaining score, average, favorite segments)
- VS divider with game status
- Current throws display
- Checkout suggestions
- Winner announcement
- Next game preview
- Walk-On button (quarter-finals+)
- YouTube modal

**Touch Targets:**
- Walk-On buttons (mobile): `padding: '6px 10px'`, no minHeight VIOLATION (lines 198, 262)
- Walk-On buttons (desktop): `minHeight: 44` VIOLATION (lines 222, 283)
- YouTube modal close — click on backdrop (no explicit button)

**Hardcoded Colors:**
- [ ] `linear-gradient(135deg, #5DD5FF, #1E7FEB)` used directly instead of `var(--pe-gradient)` VIOLATION (lines 201, 211, 252, 272)

**Tap Depth:** 1 tap (Home > Board card)

---

### 1.7 GastronomyPage (`/gastronomy`)
**Route:** Shell route
**Purpose:** NFC order + cash register system

**Required Elements:**
- Login gate (GastronomyLogin)
- View toggle: Bestellung / Kasse
- NFC scanner
- Guest search + "+ Neu" button
- Guest list
- Guest header with status + lock/unlock
- Product grid with category filter
- Cart with quantity controls
- Order submission
- Cash register view (guest orders, settle buttons)
- Settle confirmation dialog

**Touch Targets:**
- [x] View toggle buttons — `minHeight: '64px'` OK
- [x] Category filter buttons — `minHeight: '64px'` OK
- [x] Submit order button — `minHeight: '64px'` OK
- [ ] Login inputs — `minHeight: '56px'` VIOLATION (GastronomyPage.jsx:34)
- [ ] Guest search input — `minHeight: '48px'` VIOLATION (line 352)
- [ ] "+ Neu" button — `minHeight: '48px'` VIOLATION (line 360)
- [ ] Guest list buttons — `minHeight: '52px'` VIOLATION (line 374)
- [ ] SettleConfirmDialog buttons — `minHeight: '56px'` VIOLATION (lines 70, 73)
- [ ] "Jetzt abrechnen" button — `minHeight: '56px'` VIOLATION (line 638)
- [ ] Cart remove button — `w-8 h-8` (32px) CRITICAL VIOLATION (line 547)
- [ ] Guest close button — `minHeight: '44px'` VIOLATION (line 427)

**States:**
| Element | Normal | Loading | Error | Disabled | Success |
|---------|--------|---------|-------|----------|---------|
| Login | Form | "Anmelden..." + opacity | Error text | - | Redirect |
| Order btn | Gradient | "Wird gespeichert..." | Toast | Greyed (blocked/empty) | Toast |
| Settle btn | Green | - | Toast | Greyed | Success animation |
| Guest lock | Red/Green | "Wird gesperrt/entsperrt..." | Toast | opacity 0.6 | State flip |

**Tap Depth:**
- Order: Login(1) > Select guest(2) > Add product(3) = 3 taps OK
- Settle: Login(1) > Kasse tab(2) > Abrechnen(3) = 3 taps OK

---

### 1.8 NFCScanPage (`/nfc` or `/nfc-scan`)
**Route:** Standalone (no shell)
**Purpose:** NFC scan entry point

**Required Elements:**
- BackButton + logo header
- NFC scanner animation
- Error message

**Touch Targets:**
- [x] BackButton — `minHeight: '64px'` OK
- NFC scanner area — visual only

**States:**
| Element | Normal | Scanning | Error |
|---------|--------|----------|-------|
| Scanner | Pulse animation | "Wird verarbeitet..." | Error text |

**Tap Depth:** 1 tap

---

### 1.9 OrderPage (`/orders/:uid`)
**Route:** Standalone (no shell)
**Purpose:** Guest account statement (order history)

**Required Elements:**
- BackButton + logo header
- Guest info card (name, ID, active/blocked status)
- Order list (product, quantity, price, paid/open badge)
- Summary (total open, total paid)

**Touch Targets:**
- [x] BackButton — `minHeight: '64px'` OK
- No interactive elements beyond navigation

**States:**
| Element | Normal | Loading | Error |
|---------|--------|---------|-------|
| Page | Content | "Lade..." centered | Error card |

**Tap Depth:** 1 tap (from NFC scan)

---

### 1.10 RefereePage (`/referee/:boardId`)
**Route:** Standalone (no shell, full-screen scoring)
**Purpose:** Referee scoring interface

**Required Elements:**
- EmergencyLogout button (fixed top-right)
- Bulloff panel (MISS/BULL/D-Bull buttons per player)
- Segment-based throw input (number grid + modifier buttons)
- Score display
- Undo functionality
- Game finish controls
- "Zur Startseite" escape link

**Touch Targets:**
- [x] EmergencyLogout gear button — `width: 64px, height: 64px` OK
- [ ] EmergencyLogout "Abmelden" dropdown button — `minHeight: '44px'` VIOLATION (line 92)
- Bulloff buttons — check isTablet `64px` vs mobile `56px`: mobile VIOLATION
- Number/segment buttons — need verification in full file

**Tap Depth:** 2 taps (Login > Board picker > Scoring)

---

### 1.11 RefereeEntryPage (`/referee`)
**Route:** Standalone (TopBar only, no BottomNav)
**Purpose:** Referee login + board picker

**Required Elements:**
- Login form (username, password)
- Board picker list (board number, name, current game, referee status)
- "Zur Startseite" escape link

**Touch Targets:**
- [ ] Login inputs — `minHeight: '52px'` VIOLATION (line 38)
- [ ] Login button — `minHeight: '56px'` VIOLATION (line 100)
- [ ] Board cards — `padding: '14px 16px'`, no explicit minHeight — likely OK due to content height but RISKY
- "Zur Startseite" link — text link only, no minHeight VIOLATION (line 225)

**States:**
| Element | Normal | Loading | Error |
|---------|--------|---------|-------|
| Login | Form | "Anmelden..." + opacity | Error text |
| Board list | Cards | "Lade Boards..." | Toast |

**Tap Depth:** 1 tap (from login selection)

---

### 1.12 LoginSelectionPage (`/login`)
**Route:** Shell route
**Purpose:** Role selection for login

**Required Elements:**
- Three option buttons: Schiedsrichter, Gastronomy/Kasse, Admin/Veranstalter

**Touch Targets:**
- [x] Option buttons — `minHeight: '72px'` OK

**States:** No loading/error states needed (static page)

**Tap Depth:** 1 tap from Home

---

## 2. Navigation Map

```
HomePage (/)
  |
  +-- TournamentPage (/tournament/:id)         [Tab: via tournament card]
  |     +-- PlayerRegistrationPage (/tournament/:id/register)  [Link: "Beitreten"/"Jetzt anmelden"]
  |
  +-- LoginSelectionPage (/login)               [BottomNav: "Anmelden"]
  |     +-- RefereeEntryPage (/referee)         [Button: "Schiedsrichter"]
  |     |     +-- RefereePage (/referee/:boardId) [Board card click]
  |     +-- GastronomyPage (/gastronomy)        [Button: "Gastronomy/Kasse"]
  |     +-- AdminPage (/admin)                  [Button: "Admin/Veranstalter"]
  |
  +-- CurrentGameView (/board/:boardId)         [Board card in Boards tab]
  |
  +-- CancelRegistrationPage (/cancel/:token)   [Direct link from registration]
  |
  +-- NFCScanPage (/nfc, /nfc-scan)             [BottomNav for gastronomy role]
  +-- OrderPage (/orders/:uid)                  [NFC scan redirect]
```

### Back Navigation
| Screen | Back Method | Destination |
|--------|-------------|-------------|
| HomePage | - | Root page |
| TournamentPage | TopBar back arrow | Previous page (navigate(-1)) |
| PlayerRegistrationPage | TopBar back arrow | Previous page |
| CancelRegistrationPage | BackButton component | `/` |
| AdminPage | - | Root (via nav) |
| CurrentGameView | - | **MISSING** — no back navigation! |
| GastronomyPage | - | Via nav tabs |
| NFCScanPage | BackButton component | `/` |
| OrderPage | BackButton component | `/` |
| RefereePage | EmergencyLogout only | `/referee` |
| RefereeEntryPage | "Zur Startseite" link | `/` |
| LoginSelectionPage | BottomNav | `/` |

### BackButton Usage
| Page | Uses BackButton? | Notes |
|------|-----------------|-------|
| CancelRegistrationPage | Yes | to="/" |
| NFCScanPage | Yes | to="/" |
| OrderPage | Yes | to="/" |
| CurrentGameView | **No** | MISSING — user stuck on TV view without address bar in PWA |
| PlayerRegistrationPage | **No** | Uses TopBar back arrow (OK) |
| TournamentPage | **No** | Uses TopBar back arrow (OK) |

---

## 3. Component Checklists

### Global Components

#### AppShell (`components/AppShell.jsx`)
- [x] Touch-Target >= 64px — N/A (container)
- [x] Verdana, Geneva, sans-serif
- [x] Only CSS Tokens --pe-*
- [x] Dark Mode: no bg-white
- [x] Sub-page detection for nav hiding

#### TopBar (`components/TopBar.jsx`)
- [ ] Touch-Target >= 64px — Avatar button 36x36px VIOLATION (line 132)
- [ ] Touch-Target >= 64px — Logout button minHeight 44px VIOLATION (line 185)
- [x] Verdana, Geneva, sans-serif
- [x] Only CSS Tokens --pe-*
- [x] Dark Mode consistent

#### BottomNav (`components/BottomNav.jsx`)
- [x] Touch-Target >= 64px — height 80px, flex items fill height OK
- [x] Verdana, Geneva, sans-serif
- [x] Only CSS Tokens --pe-*
- [x] Dark Mode consistent
- [x] Safe area inset support

#### TopNav (`components/TopNav.jsx`)
- [ ] Touch-Target >= 64px — height 48px VIOLATION (line 62)
- [x] Verdana, Geneva, sans-serif
- [x] Only CSS Tokens --pe-*
- [x] Dark Mode consistent

#### BackButton (`components/BackButton.jsx`)
- [x] Touch-Target >= 64px — minHeight: '64px' OK
- [x] Verdana, Geneva, sans-serif
- [x] Only CSS Tokens --pe-*
- [x] Hover state present

#### Toaster (`components/Toaster.jsx`)
- [x] Touch-Target >= 64px — dismiss button 64x64 OK
- [x] Verdana, Geneva, sans-serif
- [x] Only CSS Tokens --pe-*
- [x] Dark Mode consistent
- [x] Accessible: role="alert", aria-live="assertive"

#### NFCScanner (`components/nfc/NFCScanner.jsx`)
- [x] Dark Mode consistent
- [x] Only CSS Tokens --pe-*
- [x] Loading animation (pulse)
- [ ] No error state displayed (silently fails)

### Game Components

#### Scoreboard (`components/game/Scoreboard.jsx`)
- [x] Only CSS Tokens --pe-*
- [x] Dark Mode consistent
- No interactive elements (display only)

#### ThrowInput (`components/game/ThrowInput.jsx`)
- [x] Numpad keys — minHeight: '64px' OK
- [ ] Double Out toggle — minHeight: '48px' VIOLATION (line 62)
- [x] Display area — minHeight: '64px' OK
- [x] Verdana font
- [x] Only CSS Tokens

#### BulloffScreen (`components/game/BulloffScreen.jsx`)
- [x] Score buttons — minHeight: '64px' OK
- [x] Verdana font
- [x] Only CSS Tokens

#### CheckoutSuggestions (`components/game/CheckoutSuggestions.jsx`)
- Non-interactive badges — OK
- [x] Only CSS Tokens

### Order Components

#### ProductList (`components/order/ProductList.jsx`)
- [ ] Category filter buttons — no minHeight, ~40px VIOLATION (line 23)
- [ ] "+ Warenkorb" button — minHeight: '48px' VIOLATION (line 52)
- [x] Verdana font
- [x] Only CSS Tokens

#### Cart (`components/order/Cart.jsx`)
- [x] "Bestellen" button — minHeight: '64px' OK
- [ ] Remove "-" button — w-8 h-8 (32px) CRITICAL VIOLATION (line 24)
- [x] Verdana font
- [x] Only CSS Tokens

### Admin Components

#### AdminLogin (`components/admin/AdminLogin.jsx`)
- [x] Input fields — minHeight: '64px' OK
- [x] Submit button — minHeight: '64px' OK
- [x] Verdana font
- [x] Only CSS Tokens

#### TournamentManager (`components/admin/TournamentManager.jsx`)
- [ ] Inputs — minHeight: '48px' VIOLATION (line 44)
- [ ] "+ Neu" button — no minHeight VIOLATION (line 68)
- [ ] "Turnier erstellen" button — minHeight: '48px' VIOLATION (line 116)
- [ ] "Turnier starten" button — minHeight: '48px' VIOLATION (line 143)
- [x] Verdana font
- [x] Only CSS Tokens

#### OrderSummary (`components/admin/OrderSummary.jsx`)
- No interactive elements (display only)
- [x] Only CSS Tokens
- [x] Loading/error states present

#### Bracket (`components/tournament/Bracket.jsx`)
- Non-interactive display
- [x] Only CSS Tokens
- [ ] PlayerSlot padding small (`px-3 py-2`) but non-interactive OK

---

## 4. Known UX Problems (Sorted by Severity)

### CRITICAL — Touch Targets Under 64px

| # | File | Line | Element | Current Size | Required |
|---|------|------|---------|-------------|----------|
| 1 | `components/order/Cart.jsx` | 24 | Remove "-" button | 32x32px (w-8 h-8) | 64px |
| 2 | `pages/GastronomyPage.jsx` | 547 | Cart remove "-" button | 32x32px (w-8 h-8) | 64px |
| 3 | `components/TopBar.jsx` | 132 | Avatar button | 36x36px | 64px |
| 4 | `components/TopNav.jsx` | 62 | Nav container height | 48px | 64px |

### HIGH — Touch Targets Between 44-63px

| # | File | Line | Element | Current Size |
|---|------|------|---------|-------------|
| 5 | `pages/HomePage.jsx` | 511 | Tab buttons | 48px |
| 6 | `pages/GastronomyPage.jsx` | 34 | Login inputs | 56px |
| 7 | `pages/GastronomyPage.jsx` | 352 | Guest search input | 48px |
| 8 | `pages/GastronomyPage.jsx` | 360 | "+ Neu" button | 48px |
| 9 | `pages/GastronomyPage.jsx` | 374 | Guest list buttons | 52px |
| 10 | `pages/GastronomyPage.jsx` | 70,73 | Settle dialog buttons | 56px |
| 11 | `pages/GastronomyPage.jsx` | 638 | "Jetzt abrechnen" button | 56px |
| 12 | `pages/GastronomyPage.jsx` | 427 | Guest close button | 44px |
| 13 | `pages/RefereeEntryPage.jsx` | 38 | Login inputs | 52px |
| 14 | `pages/RefereeEntryPage.jsx` | 100 | Login button | 56px |
| 15 | `pages/RefereePage.jsx` | 92 | Emergency logout dropdown | 44px |
| 16 | `pages/PlayerRegistrationPage.jsx` | 120 | Success "Zur Turnier-Ubersicht" | 56px |
| 17 | `pages/CurrentGameView.jsx` | 198,262 | Walk-On buttons (mobile) | ~36px |
| 18 | `pages/CurrentGameView.jsx` | 222,283 | Walk-On buttons (desktop) | 44px |
| 19 | `pages/TournamentPage.jsx` | 49 | "Jetzt anmelden" link | ~48px (py-3) |
| 20 | `components/TopBar.jsx` | 185 | Logout dropdown button | 44px |
| 21 | `components/game/ThrowInput.jsx` | 62 | Double Out toggle | 48px |
| 22 | `components/order/ProductList.jsx` | 23 | Category buttons | ~40px |
| 23 | `components/order/ProductList.jsx` | 52 | "+ Warenkorb" button | 48px |
| 24 | `components/admin/TournamentManager.jsx` | 44 | Form inputs | 48px |
| 25 | `components/admin/TournamentManager.jsx` | 68 | "+ Neu" button | ~40px |
| 26 | `components/admin/TournamentManager.jsx` | 116 | "Turnier erstellen" | 48px |
| 27 | `components/admin/TournamentManager.jsx` | 143 | "Turnier starten" | 48px |

### MEDIUM — Hardcoded Colors (Should Use CSS Tokens)

| # | File | Line | Value | Should Be |
|---|------|------|-------|-----------|
| 28 | `pages/CurrentGameView.jsx` | 201 | `linear-gradient(135deg, #5DD5FF, #1E7FEB)` | `var(--pe-gradient)` |
| 29 | `pages/CurrentGameView.jsx` | 211 | `linear-gradient(135deg, #5DD5FF, #1E7FEB)` | `var(--pe-gradient)` |
| 30 | `pages/CurrentGameView.jsx` | 252 | `linear-gradient(135deg, #5DD5FF, #1E7FEB)` | `var(--pe-gradient)` |
| 31 | `pages/CurrentGameView.jsx` | 272 | `linear-gradient(135deg, #5DD5FF, #1E7FEB)` | `var(--pe-gradient)` |

### LOW — Navigation & Missing States

| # | File | Issue |
|---|------|-------|
| 32 | `pages/CurrentGameView.jsx` | No back navigation — user trapped in PWA mode |
| 33 | `pages/PlayerRegistrationPage.jsx` | Success "Link offnen" link has no minHeight (line 112) |
| 34 | `pages/CancelRegistrationPage.jsx` | "Zur Startseite" links (lines 65, 136, 155) have no minHeight |
| 35 | `pages/RefereeEntryPage.jsx` | "Zur Startseite" link is text-only, no minHeight (line 225) |
| 36 | `components/nfc/NFCScanner.jsx` | NFC error state sets status but never displays error message to user |

---

## 5. Summary

- **Total screens analyzed:** 12
- **Total components analyzed:** 17
- **Touch target violations found:** 27
- **Hardcoded color violations:** 4
- **Navigation issues:** 5
- **Total UX problems:** 36
